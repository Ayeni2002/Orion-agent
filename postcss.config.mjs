/**
 * Tailwind CSS v4 is configured entirely here and in `src/app/globals.css`.
 * v4 has no `tailwind.config.js` — theme values live in the `@theme` block.
 */
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
